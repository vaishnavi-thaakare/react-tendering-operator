import React from "react";

const First = (props) => {
  return (
    <div>
      <h1> Hi from first</h1>
      <button onClick={props.pratik}> First Button </button>
    </div>
  );
};

export default First;
