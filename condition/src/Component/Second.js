import React from "react";

const Second = (props) => {
  return (
    <div>
      <h1>Hi from Second</h1>
      <button onClick={props.neha}> Second Button </button>
    </div>
  );
};

export default Second;
